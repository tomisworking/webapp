name: Prowler Security Audit

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    # Run weekly on Monday at 9:00 AM
    - cron: '0 9 * * 1'
  workflow_dispatch:  # Manual trigger

permissions:
  id-token: write   # Required for OIDC
  contents: read
  security-events: write

env:
  AWS_REGION: us-east-1

jobs:
  prowler-audit:
    name: Prowler AWS Security Audit
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::311603531332:role/GitHubActionsRole
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Run Prowler AWS Security Audit
        uses: prowler-cloud/prowler@main
        with:
          # Scan entire AWS account
          command: 'aws'
          # Focus on CIS AWS Foundations Benchmark
          checks: 'cis_1.5_aws'
          # Generate multiple report formats
          output-formats: 'html,json,csv'
          # Output directory
          output-directory: 'prowler-reports'
      
      - name: Upload Prowler Reports
        uses: actions/upload-artifact@v3
        if: always()
        with:
          name: prowler-reports
          path: prowler-reports/
          retention-days: 30
      
      - name: Parse Prowler Results
        if: always()
        run: |
          # Parse JSON report for summary
          if [ -f prowler-reports/prowler-output.json ]; then
            TOTAL=$(jq '[.[] | select(.Status)] | length' prowler-reports/prowler-output.json)
            PASSED=$(jq '[.[] | select(.Status=="PASS")] | length' prowler-reports/prowler-output.json)
            FAILED=$(jq '[.[] | select(.Status=="FAIL")] | length' prowler-reports/prowler-output.json)
            
            if [ $TOTAL -gt 0 ]; then
              COMPLIANCE=$(awk "BEGIN {printf \"%.1f\", ($PASSED/$TOTAL)*100}")
            else
              COMPLIANCE="0.0"
            fi
            
            echo "### 🔒 Prowler Security Audit Results" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**CIS AWS Foundations Benchmark v1.5.0**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Metric | Value |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| ✅ Passed | $PASSED |" >> $GITHUB_STEP_SUMMARY
            echo "| ❌ Failed | $FAILED |" >> $GITHUB_STEP_SUMMARY
            echo "| 📊 Total Checks | $TOTAL |" >> $GITHUB_STEP_SUMMARY
            echo "| 🎯 Compliance | $COMPLIANCE% |" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            # List critical failures
            echo "### ⚠️ Critical Findings" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            jq -r '.[] | select(.Status=="FAIL" and .Severity=="critical") | "- **\(.CheckID)**: \(.CheckTitle)"' prowler-reports/prowler-output.json | head -10 >> $GITHUB_STEP_SUMMARY || echo "No critical findings" >> $GITHUB_STEP_SUMMARY
          fi
      
      - name: Check Compliance Threshold
        run: |
          if [ -f prowler-reports/prowler-output.json ]; then
            TOTAL=$(jq '[.[] | select(.Status)] | length' prowler-reports/prowler-output.json)
            PASSED=$(jq '[.[] | select(.Status=="PASS")] | length' prowler-reports/prowler-output.json)
            
            if [ $TOTAL -gt 0 ]; then
              COMPLIANCE=$(awk "BEGIN {printf \"%.1f\", ($PASSED/$TOTAL)*100}")
              THRESHOLD=80
              
              if (( $(echo "$COMPLIANCE >= $THRESHOLD" | bc -l) )); then
                echo "✅ Compliance ($COMPLIANCE%) meets threshold ($THRESHOLD%)"
              else
                echo "⚠️ Compliance ($COMPLIANCE%) below threshold ($THRESHOLD%)"
                exit 1
              fi
            fi
          fi
      
      - name: Comment PR with Results
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            if (fs.existsSync('prowler-reports/prowler-output.json')) {
              const data = JSON.parse(fs.readFileSync('prowler-reports/prowler-output.json'));
              const total = data.filter(r => r.Status).length;
              const passed = data.filter(r => r.Status === 'PASS').length;
              const failed = data.filter(r => r.Status === 'FAIL').length;
              const compliance = total > 0 ? ((passed/total)*100).toFixed(1) : 0;
              
              const comment = `## 🔒 Prowler Security Audit Results
              
              **CIS AWS Foundations Benchmark v1.5.0**
              
              | Metric | Value |
              |--------|-------|
              | ✅ Passed | ${passed} |
              | ❌ Failed | ${failed} |
              | 📊 Total | ${total} |
              | 🎯 Compliance | ${compliance}% |
              
              ${compliance >= 80 ? '✅ Compliance meets threshold (80%)' : '⚠️ Compliance below threshold (80%)'}
              
              [View detailed report in artifacts](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
              `;
              
              github.rest.issues.createComment({
                issue_number: context.issue.number,
                owner: context.repo.owner,
                repo: context.repo.repo,
                body: comment
              });
            }
